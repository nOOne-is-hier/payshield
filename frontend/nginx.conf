server {
  listen 80;
  listen [::]:80;

  root /usr/share/nginx/html;
  index index.html;

  # SPA 라우팅: 존재하지 않는 경로는 index.html로
  location / {
    try_files $uri $uri/ /index.html;
  }

  # (중요) 프록시는 여기서 하지 않음. /api 라우팅은 Ingress가 수행.
}
